// Re-exports from AuthContext.tsx — single source of truth for auth
export { AuthProvider, useAuth, AuthContext } from "./AuthContext";
export type { UserProfile, UserRole, AuthContextValue } from "./AuthContext";
